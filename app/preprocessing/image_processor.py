def preprocess_image(image_path: str):
    """Redresse, recadre, débruite et binarise une image de CNI.

    Args:
        image_path: chemin vers l'image brute (recto ou verso).

    Returns:
        Un tableau NumPy représentant l'image prétraitée.
    """
    raise NotImplementedError("preprocess_image n'est pas encore implémenté")